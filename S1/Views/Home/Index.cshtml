@model IEnumerable<S1.Models.Autor>     

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutPage1.cshtml";
}

<h2>Index</h2>
@*<a href="@Url.Content("/Home/Create_A")">New</a>*@

<p>
    @Html.ActionLink("Nowy Autor", "Create_A")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Imie)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nazwisko)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Data_ur)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Imie)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nazwisko)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data_ur)
            </td>
            <td>
                @Html.ActionLink("Edytuj", "Edit_A", new { id_a = item.Id_A }) |
                @Html.ActionLink("Detale", "Detale_Autora", new { id_a = item.Id_A}) |
                @Html.ActionLink("Usun", "Delete_A", new { id_a = item.Id_A})
            </td>
        </tr>
    }

</table>
